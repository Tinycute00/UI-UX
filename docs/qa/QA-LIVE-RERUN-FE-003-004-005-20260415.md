# FE-003/004/005 Live API Integration QA Report — RERUN

**Date:** 2026-04-15  
**Workspace:** `/home/beer8/team-workspace/UI-UX`  
**Frontend HEAD:** `2c18541`  
**Backend HEAD:** `916c0b1`

## Final verdict

| Feature | Status | Root cause category |
|---|---|---|
| FE-003 Dashboard | **BLOCKED** | backend contract gap |
| FE-004 Billing | **PASS** | none |
| FE-005 Safety | **PASS** | none |

## Evidence summary

### FE-003 Dashboard
**Frontend mapping is correct**, but the live dashboard initialization depends on three API calls:
- `GET /api/v1/projects/101/progress` → **200**
- `GET /api/v1/projects/101/work-items` → **404 NOT_FOUND**
- `GET /api/v1/projects/101/subcontractors` → **404 NOT_FOUND**

Verified by browser runtime:
- `src/app/dashboard-init.js` calls `Promise.all([getDashboardData(), getWorkItems(), getSubcontractors()])`
- `src/api/adapters/dashboard-adapter.js` points to the correct endpoints
- Browser landed on dashboard error state with message: **「找不到請求的資料」**

**Conclusion:** FE-003 is **BLOCKED** because the backend contract is incomplete for the dashboard’s required live data set. This is not a browser bug and not a frontend mapping bug.

### FE-004 Billing
Live verification succeeded:
- `GET /api/v1/valuations?status=pending` → **200**
- `GET /api/v1/valuations?status=pending_review` → **200**
- Backend alias handling maps `pending` / `submitted` → `pending_review`
- Frontend adapter passes `status` through correctly

**Conclusion:** FE-004 is **PASS**.

### FE-005 Safety
Live verification succeeded:
- `POST /api/v1/safety-inspections` with valid payload → **201**
- Request body accepted and returned persisted inspection payload
- Frontend `safety.js` posts to the correct endpoint and validates input before submit

**Conclusion:** FE-005 is **PASS**.

## Residual issue classification

- **Browser behavior:** no blocker observed
- **Frontend mapping:** no blocker observed for FE-004 / FE-005; FE-003 mapping is correct
- **Backend contract:** FE-003 is missing `GET /api/v1/projects/:id/work-items` and `GET /api/v1/projects/:id/subcontractors`, which blocks full dashboard closure

## Request to Tiny

<@666614102734405632> 需要協助：FE-003 Dashboard 仍被 backend contract 阻塞。現階段 progress endpoint 已通，但 dashboard 依賴的 `work-items` / `subcontractors` 兩個 live route 回 404，請協助 Backend 對齊這兩個 contract。其餘 FE-004 / FE-005 已可關閉。

## PM report target

`discord:1491771769072255208:1493781212853178458`

## Closing note

**無特別協助需求，持續驗收中。**